const alumno =`Pepe:`;
const asistencia = `ok`;
console.log(asistencia.repeat(5));
console.log(`${alumno}${asistencia}`.repeat(5));


document.querySelector(`h2`).innerHTML= `${alumno}${asistencia}`.repeat(5);

const curvaFrotend = `html css javascrit react`
console.log(curvaFrotend.split(" "));

console.log(curvaFrotend.split(""))

document.querySelector(`h3`).innerHTML= `${curvaFrotend.split(" ")}`
document.querySelector(`h4`).innerHTML= `${curvaFrotend.split("")}`;
